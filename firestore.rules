rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public access for development as the app uses a custom local auth state
    // In production, please enable Firebase Auth and use request.auth != null
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
